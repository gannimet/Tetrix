package de.riwo.tetrix.tetris;

public interface LevelChangeListener {

	/**
	 * Called whenever the game level changes.
	 * @param level the new level which the game has entered.
	 */
	public void onLevelChanged(int level);
	
}