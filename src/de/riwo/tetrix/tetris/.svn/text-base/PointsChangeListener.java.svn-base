package de.riwo.tetrix.tetris;

public interface PointsChangeListener {

	/**
	 * Called whenever the player's number of points has changed.
	 * @param points the new number of points
	 */
	public void onPointsChanged(int points);
	
}